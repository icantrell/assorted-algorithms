This is a quadratic sieve. An algorithm for factoring large prime numbers. I read about this algorithm from wikipedia. It current uses the fermat method, which makes it an order slower from a quadratic sieve that can factor a small RSA number.

Also xcgd.py is copied from another website.

